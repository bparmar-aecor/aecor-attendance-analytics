# Copy this to .streamlit/secrets.toml and fill in your service-role key.
# DO NOT commit secrets.toml to version control.
#
# Use the SERVICE-ROLE key (NOT anon key) — the dashboard writes to
# employee_categories, employee_leaves, and punch_regularizations.

[supabase]
url = "https://voawhdyaxdivegcncoeq.supabase.co"
key = "<paste your service-role key here>"
